@model IEnumerable<ImprovedWorkCenter.Models.Actividad>

@{
    ViewData["Title"] = "Index";
}

<div class="description">
    <h1>Actividades</h1>
    <p>
        <a asp-action="Create">Agregar Actividad</a>
    </p>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorarioFinal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorarioFinal)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActividadId">Editar Actividad</a> |
                <a asp-action="Details" asp-route-id="@item.ActividadId">Detalles de Actividad</a> |
                <a asp-action="Delete" asp-route-id="@item.ActividadId">Eliminar Actividad</a>
            </td>
        </tr>
}
    </tbody>
</table>
